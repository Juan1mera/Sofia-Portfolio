---
// ContactSection - Editorial contact layout
---

<section id="contact" class="contact-section">
  <div class="container">
    <!-- Opening headline -->
    <div class="contact-headline">
      <h2 class="headline-text">
        Let's create<br />something <em>together.</em>
      </h2>
      <p class="headline-sub">
        I'm always open to new projects, collaborations, and creative
        conversations. Whether you have a bold idea or just want to say hello —
        I'd love to hear from you.
      </p>
    </div>

    <!-- Two large social links side by side -->
    <div class="social-duo">
      <a
        href="#"
        class="social-large"
        target="_blank"
        rel="noopener noreferrer"
      >
        <span class="at-sign">@</span>
        <span class="social-name">Behance</span>
      </a>
      <a
        href="#"
        class="social-large"
        target="_blank"
        rel="noopener noreferrer"
      >
        <span class="at-sign">@</span>
        <span class="social-name">Instagram</span>
      </a>
    </div>

    <!-- Secondary links row -->
    <div class="secondary-links">
      <a href="#" class="sec-link">Telegram</a>
      <span class="link-dot"></span>
      <a href="#" class="sec-link">WhatsApp</a>
      <span class="link-dot"></span>
      <a href="mailto:sofia@ejemplo.com" class="sec-link">Email</a>
      <span class="link-dot"></span>
      <a href="#" class="sec-link">LinkedIn</a>
    </div>

    <!-- Closing bold statement -->
    <div class="closing-statement">
      <h3 class="closing-text">Later means never.</h3>
      <p class="closing-sub">
        Reach out for <a href="mailto:sofia@ejemplo.com">a quick chat</a>
        or connect with me on
        <a href="#" target="_blank" rel="noopener noreferrer">LinkedIn</a>. If
        you'd like to explore more of my work,
        <a href="#artist">start here</a>. Thanks for scrolling all the way down.
        See you soon.
      </p>
    </div>
  </div>
</section>

<style>
  .contact-section {
    min-height: 100vh;
    padding: 8rem 2rem 5rem;
    background-color: var(--color-bg-primary, #0a0a0a);
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
  }

  .contact-section::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
      radial-gradient(
        circle at 20% 30%,
        rgba(227, 202, 171, 0.04) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 80% 70%,
        rgba(227, 202, 171, 0.03) 0%,
        transparent 50%
      );
    pointer-events: none;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
    width: 100%;
  }

  /* ── Opening headline ── */
  .contact-headline {
    margin-bottom: 5rem;
    animation: fadeUp 1s ease-out;
  }

  .headline-text {
    font-family: "Alfa Slab One", serif;
    font-size: clamp(2.5rem, 6vw, 5rem);
    font-weight: 400;
    color: var(--color-text-primary, #f5f5f5);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .headline-text em {
    color: var(--color-accent, #e3caab);
    font-style: normal;
  }

  .headline-sub {
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    line-height: 1.8;
    color: var(--color-text-muted, #888888);
    max-width: 600px;
    font-weight: 300;
  }

  /* ── Two large social links ── */
  .social-duo {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
    animation: fadeUp 1s ease-out 0.2s both;
  }

  .social-large {
    display: flex;
    align-items: baseline;
    gap: 0.25rem;
    text-decoration: none;
    color: var(--color-accent, #e3caab);
    font-family: "Alfa Slab One", serif;
    font-size: clamp(3rem, 8vw, 7rem);
    font-weight: 400;
    line-height: 1;
    transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    position: relative;
    padding-bottom: 0.5rem;
  }

  .social-large::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: var(--color-accent, #e3caab);
    transition: width 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .social-large:hover::after {
    width: 100%;
  }

  .social-large:hover {
    color: var(--color-text-primary, #f5f5f5);
    transform: translateX(8px);
  }

  .at-sign {
    font-size: 0.6em;
    opacity: 0.5;
    transition: opacity 0.4s ease;
  }

  .social-large:hover .at-sign {
    opacity: 1;
  }

  .social-name {
    display: inline-block;
  }

  /* ── Secondary links row ── */
  .secondary-links {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    flex-wrap: wrap;
    margin-bottom: 6rem;
    animation: fadeUp 1s ease-out 0.4s both;
  }

  .sec-link {
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    color: var(--color-text-muted, #888888);
    text-decoration: none;
    font-weight: 400;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    transition: all 0.3s ease;
    position: relative;
  }

  .sec-link::after {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--color-accent, #e3caab);
    transition: width 0.3s ease;
  }

  .sec-link:hover {
    color: var(--color-accent, #e3caab);
  }

  .sec-link:hover::after {
    width: 100%;
  }

  .link-dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--color-text-dim, #555555);
    flex-shrink: 0;
  }

  /* ── Closing statement ── */
  .closing-statement {
    border-top: 1px solid rgba(227, 202, 171, 0.1);
    padding-top: 4rem;
    animation: fadeUp 1s ease-out 0.6s both;
  }

  .closing-text {
    font-family: "Alfa Slab One", serif;
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 400;
    color: var(--color-text-primary, #f5f5f5);
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }

  .closing-sub {
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    line-height: 1.9;
    color: var(--color-text-muted, #888888);
    max-width: 650px;
    font-weight: 300;
  }

  .closing-sub a {
    color: var(--color-accent, #e3caab);
    text-decoration: none;
    border-bottom: 1px solid rgba(227, 202, 171, 0.3);
    transition: all 0.3s ease;
  }

  .closing-sub a:hover {
    border-bottom-color: var(--color-accent, #e3caab);
    color: var(--color-text-primary, #f5f5f5);
  }

  /* ── Animations ── */
  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ── Responsive ── */
  @media (max-width: 768px) {
    .contact-section {
      padding: 5rem 1.5rem 3rem;
    }

    .contact-headline {
      margin-bottom: 3rem;
    }

    .social-duo {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .social-large {
      font-size: clamp(2.5rem, 12vw, 4rem);
    }

    .secondary-links {
      margin-bottom: 4rem;
    }

    .closing-statement {
      padding-top: 3rem;
    }
  }
</style>
